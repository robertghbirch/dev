clear

echo "compiling.... (short file)"

time gcc -O0 main.c utils.c p_wire_protocol.pb-c.c protobuf-c.c -o my_program -pthread -I/usr/local/include -pthread -L/usr/local/lib -lpthread 
#-lprotobuf-c

if [ $? -eq 0 ] 
then
  echo
  echo "GENIUS!"

  echo 
  echo "  DEVICE DEVICE DEVICE"
  echo " DEVICE DEVICE DEVICE"
  echo "DEVICE DEVICE DEVICE"
  echo

  echo "BOTH WAYS"
  echo "DONE"

else

  echo
  echo "ERROR!"

fi
echo
